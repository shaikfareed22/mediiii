name: Removed - use Vercel

# This workflow was removed in favor of deploying to Vercel.
# If you still want GitHub Pages, recreate a workflow that
# builds a serverless-free static export and publishes ./out.
